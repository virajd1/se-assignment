{
  "name": "se-assessment-node",
  "version": "0.1.0",
  "description": "Node.js workspace for the SE assessment API puzzle.",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "serve": "node src/server.js",
    "health": "node src/index.js --health",
    "discover": "node src/index.js --discover",
    "fetch": "node src/index.js --fetch-dataset",
    "submit": "node src/index.js --submit",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
